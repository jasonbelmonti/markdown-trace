# EP-GATE-1 independent corpus interpretation

Read first: `docs/tasks/document-graph-contract.md` and its checksum, then `.codefactory/execution-plans/document-graph-contract-closeout/execution-plan.md` and its checksum. The task is revision 3, READY/DIRECT; the plan remains revision 1, READY. This gate serves TD-SC-1 and the language/ownership portion of TD-SC-3. The user's current authorization is one PR for EP-ACT-2 and EP-GATE-1. Parent task completion, EP-ACT-3 full API results/checker, runtime, public exports and pilot measurements are deferred.

Read `.codefactory/execution-plans/document-graph-contract-closeout/evidence/gate-1/inputs.json`, verify its packet/source hashes, then read the entire named interface packet and every listed Markdown source. The packet owns semantics and its companion declarations own public shape. Use the input inventory for case membership and scenario inputs only. Do not read the corpus manifest, cases.md, any expected JSON, authoring scripts or another reviewer's report during the independent pass. Public Markdown Engine 3.5.0 may be used to confirm actual node structure/raw slices, never to compute Trace semantics. No parallel Markdown parser or extractor may generate expected facts.

Independently derive every case's declarations, references/relationship kinds, owner declaration(s), malformed/literal exclusions, missing/duplicate/unknown-label and unowned/ambiguous-reference integrity observations, completeness, and context ownership/structural support. Interpret all 55 LANG cases and all 24 CASE scenarios. Shared scenario source may reuse an explicitly named independent source interpretation; still record the scenario's own input and expected semantic/error outcome. CASE-10 also has a separate after-edit source. Language cases use REQ=requirement, WP=work and VAL=validation; no required-relation assertions are needed to judge lexical/ownership integrity. Keep exact runtime diagnostic messages/codes unspecified where the packet does not define them.

Write one JSON report to the reviewer path provided in the assignment. It must contain reviewer identity, input hashes, 79 explicit case entries, and disagreements/questions. For each distinct source, enumerate occurrences in source order using local O1/O2 aliases, label, role, exact raw spelling (and offset if useful); for each reference name its relation and owner declaration aliases, using [] for unowned and multiple aliases for ambiguous. Record exclusions and fragment ownership using exact source snippets or ranges, and diagnostic/coverage outcomes. Case-specific observations must distinguish plausible errors such as first-wins definitions, salvaged malformed targets, leaked scope or dropped unresolved edges. This first report is interpretation evidence, not approval of unseen annotations.

After writing and reading your independent report, send its path and SHA-256 to the parent and stop. A subsequent comparison pass will open the authored annotations and reconcile each case against this frozen report. Only after that comparison can this gate pass. Any disputed semantic case requires both reviewers to recheck the corrected input. Preserve before/after fingerprints and disagreement resolutions. Missing required cases, self-derived expectations, wrong roles/owners/ranges, contradictory language or unsupported completion claims block this gate. Deferred runtime/API expansion does not block this corpus PR unless the current corpus introduces a correctness or maintainability defect inside this boundary.
