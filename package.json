{
  "name": "markdown-trace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "markdown-trace": "./dist/markdowntrace/cli.js"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "npm run generate:release-metadata --silent && tsc -p tsconfig.build.json",
    "generate:release-metadata": "node scripts/generate-release-metadata.mjs",
    "check:release-metadata": "node scripts/generate-release-metadata.mjs --check",
    "test": "vitest run \"--exclude=.worktrees/**\"",
    "typecheck": "tsc --noEmit",
    "ci:enforcement": "bash scripts/ci-enforcement.sh",
    "derive:fixture": "npm run build --silent && node dist/markdowntrace/cli.js derive --document fixtures/r0-document-local-registry/execution-spec.md --namespace exec",
    "validate:fixture": "npm run build --silent && node dist/markdowntrace/cli.js validate --registry fixtures/r0-document-local-registry/entity-registry.yaml --document fixtures/r0-document-local-registry/execution-spec.md --report docs/evidence/valid-fixture-report.md",
    "migration:check": "npm run build --silent && node dist/markdowntrace/cli.js migration-check --document fixtures/r1-link-backed-entity-syntax/minimal-link-backed-execution-spec.md --manual-registry fixtures/r1-link-backed-entity-syntax/minimal-link-backed-manual-registry.yaml --type-profile fixtures/r1-link-backed-entity-syntax/minimal-type-profile.yaml"
  },
  "dependencies": {
    "@jasonbelmonti/markdown-engine": "3.5.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
